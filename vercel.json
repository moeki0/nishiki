{
  "buildCommand": "cd apps/unfold && npm run build",
  "outputDirectory": "apps/unfold/.next",
  "installCommand": "npm install && cd apps/unfold && npm install --legacy-peer-deps",
  "framework": "nextjs"
}
